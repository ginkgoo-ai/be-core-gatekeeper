package com.ginkgooai.core.gatekeeper.domain.types;

public enum OptionsSourceType {

	STATIC, // Options are defined directly in the metadata
	DYNAMIC_API // Options are fetched from an API endpoint

}
