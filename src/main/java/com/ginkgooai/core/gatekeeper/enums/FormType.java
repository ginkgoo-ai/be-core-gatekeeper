package com.ginkgooai.core.gatekeeper.enums;

public enum FormType {

	QUESTIONNAIRE, // For forms where results are stored internally as questionnaire
					// responses
	GENERIC_FORM // For forms that might submit to external services or have other generic
					// handling
	// Add other types as needed in the future

}